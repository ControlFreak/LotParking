# Add script to:
# * Install dependencies
# * Build/Compile
# * Run Test Suit to validate
#
# After this is run, bin/parking_lot
# should Just Work.


# install ruby 2.4.1 with
# create gemset
# install bundler
# do bundle install